#!/usr/bin/env node

const fs = require('fs');
const path = require('path');

module.exports = function(ctx) {
    console.log('✓ Splash screen hook disabled - no custom splash screen');
    // This hook is now inactive - no splash screen setup
};
